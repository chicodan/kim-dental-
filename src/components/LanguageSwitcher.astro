---
// Temporarily disabled - Korean not ready for launch
// This component will be re-enabled when Korean localization is ready

// import { getLocale } from "../paraglide/runtime.js";
// import { getAlternateLocale, getLocaleName } from "../i18n/locales";
// import type { Locale } from "../i18n/locales";

// const currentLocale = getLocale() as Locale;
// const alternateLocale = getAlternateLocale(currentLocale);
// const alternateLocaleName = getLocaleName(alternateLocale);

// // Get the current path without the locale prefix
// const currentPath = Astro.url.pathname;
// const pathWithoutLocale = currentPath.replace(/^\/(en|ko)/, "") || "/";
// const alternatePath = `/${alternateLocale}${pathWithoutLocale}`;
---

<!-- Temporarily disabled - Korean not ready for launch --><!-- <a
  href={alternatePath}
  class="inline-flex items-center px-3 py-1 text-sm font-medium text-gray-700 bg-white border border-gray-300 rounded-md hover:bg-gray-50 transition-colors"
  aria-label={`Switch to ${alternateLocaleName}`}
>
  <svg
    class="w-4 h-4 mr-1.5"
    fill="none"
    stroke="currentColor"
    viewBox="0 0 24 24"
    xmlns="http://www.w3.org/2000/svg"
  >
    <path
      stroke-linecap="round"
      stroke-linejoin="round"
      stroke-width="2"
      d="M3 5h12M9 3v2m1.048 9.5A18.022 18.022 0 016.412 9m6.088 9h7M11 21l5-10 5 10M12.751 5C11.783 10.77 8.07 15.61 3 18.129"
    ></path>
  </svg>
  {alternateLocaleName}
</a> -->
