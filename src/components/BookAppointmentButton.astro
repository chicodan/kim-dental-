---
import { getLocale } from "../paraglide/runtime.js";
import * as m from "../paraglide/messages.js";
import type { Locale } from "../i18n/locales";

// Force English-only mode - Korean not ready for launch
// Runtime override handled in locales.ts

const currentLocale = getLocale() as Locale;
const { className = "", href = `/${currentLocale}/book` } = Astro.props;
---

<a
  href={href}
  class={`bg-accent-teal text-white px-6 py-3 rounded shadow hover:bg-accent-teal-dark transition font-semibold ${className}`}
>
  {m.book_appointment()}
</a>
